import React from "react";
import { Form } from "react-bootstrap";
import CheckoutOfficial from "../CheckoutOfficial";

const FormCheckout = ({ label, name }) => {
  const addCheckoutHandler = (hi) => {
    console.log(hi);
  };

  return <div>
    
  </div>;
};

export default FormCheckout;
